{
  "name": "dailytipsapp-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "vite build",
    "prepare:release-resources": "powershell -ExecutionPolicy Bypass -File scripts/prepare_release_resources.ps1",
    "build:windows": "npm run prepare:release-resources && npm run tauri build",
    "build:windows:nsis": "npm run prepare:release-resources && npx tauri build --bundles nsis",
    "build:windows:msi": "npm run prepare:release-resources && npx tauri build --bundles msi",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "vue": "^3.5.13",
    "@tauri-apps/plugin-dialog": "^2.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.2.0"
  }
}
